# Backend runtime port for FastAPI/uvicorn.
API_PORT=8000

# Optional explicit host bind (inside container).
API_HOST=0.0.0.0

# Disable autoreload in production.
API_RELOAD=false

# Optional persistence (enable postgres service/profile in docker-compose if used).
DATABASE_URL=postgresql://navispace:navispace@postgres:5432/navispace

# Blueprint and generated building assets location.
BUILDING_ASSETS_PATH=/app/assets

# CORS origins for browser clients.
# Use "*" for open development mode, or a comma-separated list in production:
# NAVISPACE_CORS_ORIGINS=https://app.company.com,https://admin.company.com
NAVISPACE_CORS_ORIGINS=*

# Optional ML segmentation inference (walls + doors)
# NAVISPACE_ENABLE_ML supports: true | false | auto
# - auto: enable only if checkpoint exists (recommended)
NAVISPACE_ENABLE_ML=auto
NAVISPACE_SEG_MODEL=/app/backend/ml/checkpoints/wall_door_unet.pt
# Use cpu by default for maximum compatibility; set to mps on Apple Silicon if stable.
NAVISPACE_SEG_DEVICE=cpu
NAVISPACE_SEG_INPUT_SIZE=768
NAVISPACE_SEG_THRESHOLD=0.45
